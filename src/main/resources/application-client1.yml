spring:
  application:
    name: config-git-client
  cloud:
    bus:
      trace:
        enabled: true
      enabled: true
server:
  port: 13000
management:
  endpoints:
    web:
      exposure:
        include: "refresh"